---
// Runs before body paint so the sidebar's stored open/closed state applies
// without a flash of the opposite state on load.
---
<script is:inline>
  var v = localStorage.getItem("aiml-sidebar");
  if (v === "open" || v === "closed") document.documentElement.setAttribute("data-sidebar", v);
</script>
